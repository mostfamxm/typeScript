class Person {
    startEating(food, time) {
        return `i start eating at${time} the food i chooesed${food}`;
    }
    startSleeping(sleepTime) {
        return `i go to sleep at ${sleepTime}`;
    }
    startRunning(speed) {
        return `my run speed is ${speed}`;
    }
}
//# sourceMappingURL=multyInterface.js.map